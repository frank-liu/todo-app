# Environment variables for the Todo App
# Copy this file to `.env` (or `.env.local`) and adjust values as needed.
# CRA exposes only variables prefixed with REACT_APP_.

# Analytics endpoint for Web Vitals metrics
# In development:
# - If REACT_APP_ANALYTICS_URL is set, metrics will be POSTed there (mock SW disabled)
# - If not set, metrics are logged to the console and a mock SW accepts /analytics
# In production:
# - Metrics are always sent; if not set, falls back to /analytics

# Option A: Grafana Annotations API via CRA proxy (recommended for dev)
#   package.json includes: "proxy": "http://localhost:4000"
#   so a relative path will be forwarded to Grafana to avoid CORS.
# REACT_APP_ANALYTICS_URL=/api/annotations

# Option B: Absolute URL (requires Grafana CORS allowance or a reverse proxy in dev)
# REACT_APP_ANALYTICS_URL=http://localhost:4000/api/annotations

# Optional: Grafana service account token (adds Authorization: Bearer ...)
# REACT_APP_GRAFANA_API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

REACT_APP_ANALYTICS_URL=
