# Environment variables for the Todo App
# Copy this file to `.env` (or `.env.local`) and adjust values as needed.
# CRA exposes only variables prefixed with REACT_APP_.

# Analytics endpoint for Web Vitals metrics
# The Todo app now has a dedicated Express.js backend server that handles:
# - Web Vitals metrics collection
# - Forwarding to Grafana
# - CORS handling

# Backend server configuration:
# - Backend runs on http://localhost:4000
# - Frontend proxy in package.json forwards /api/* to backend
# - Backend forwards to Grafana on http://localhost:3001

# Option A: Use backend server via CRA proxy (recommended)
#   This uses the backend server which handles Grafana forwarding
REACT_APP_ANALYTICS_URL=/api/annotations

# Option B: Direct Grafana URL (bypasses backend - not recommended for production)
# REACT_APP_ANALYTICS_URL=http://localhost:3001/api/annotations

# Optional: Grafana service account token (configure in backend/.env instead)
# REACT_APP_GRAFANA_API_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Default configuration for using the backend server
REACT_APP_ANALYTICS_URL=/api/annotations
